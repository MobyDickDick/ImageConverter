[pytest]
testpaths = tests
norecursedirs = vendor .git .venv venv build dist
addopts = -m "not blocking_conversion"
markers =
    blocking_conversion: Tests with currently blocking conversion behaviour; excluded from default test battery and tracked in docs/open_tasks.md.
