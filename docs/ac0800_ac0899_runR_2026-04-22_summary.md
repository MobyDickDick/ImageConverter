# AC0800..AC0899 Vollbereichslauf – 2026-04-22 (Run R)

- **Datum (UTC):** 2026-04-22
- **Anlass:** Weiterer B2-Follow-up mit längerem Timeout-Fenster als Run Q, um den stabilen Frühbereich erneut zu verifizieren.
- **Befehl:**

```bash
timeout 300 python -u -m src.imageCompositeConverter \
  artifacts/images_to_convert \
  --descriptions-path artifacts/images_to_convert/Finale_Wurzelformen_V3.xml \
  --output-dir artifacts/converted_images \
  --start AC0800 \
  --end AC0899 \
  --isolate-svg-render \
  --deterministic-order
```

- **Ausführung:** mit `tee` in `artifacts/converted_images/reports/AC0800_AC0899_batch_2026-04-22_runR.log`
- **Sichtbarer Teilfortschritt im Log:** `AC0800_L`, `AC0800_M`, `AC0800_S`, anschließend Start von `AC0811_L`.
- **Crash-Beobachtung bis zum Timeout:** Kein MuPDF-`stack overflow`/Segfault im sichtbaren Abschnitt.
- **Laufstatus:** Durch `timeout 300` mit Exit-Code `124` beendet; damit weiterhin kein vollständiger Batch-Abschluss und kein finaler Exit-`0`-Nachweis für B2.

## Kurzfazit

Run R bestätigt den stabilen Startbereich erneut unter identischer Konfiguration mit aktivem Isolations-Guard. Für den Abschluss von B2 fehlt weiterhin ein vollständiger Vollbereichslauf bis `AC0899` mit stabilem Exit-Code `0`.
