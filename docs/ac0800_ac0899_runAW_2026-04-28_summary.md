# AC0800..AC0899 Vollbereichslauf – 2026-04-28 (Run AW)

- **Datum (UTC):** 2026-04-28
- **Anlass:** N1/N2 als nächste offene dokumentierte Aufgabe weiterbearbeiten und den aktuellen Vollbereichsstatus mit reproduzierbarem Exit-Code festhalten.
- **Befehl:**

```bash
set -o pipefail
timeout 300 python -u -m src.imageCompositeConverter \
  artifacts/images_to_convert \
  --descriptions-path artifacts/images_to_convert/Finale_Wurzelformen_V3.xml \
  --output-dir /tmp/imageconverter_runAW \
  --start AC0800 \
  --end AC0899 \
  --isolate-svg-render \
  --deterministic-order \
  | tee artifacts/converted_images/reports/AC0800_AC0899_batch_2026-04-28_runAW.log
```

- **Ausführung:** mit `tee` in `artifacts/converted_images/reports/AC0800_AC0899_batch_2026-04-28_runAW.log`
- **Sichtbarer Teilfortschritt im Log:** `AC0800_L`, `AC0800_M`, `AC0800_S`, `AC0811_L`.
- **Crash-Beobachtung bis zum Abbruch:** Kein MuPDF-`stack overflow`/Segfault im sichtbaren Logabschnitt.
- **Laufstatus:** Prozessende mit `Exit-Code 0`, jedoch ohne dokumentierten Vollbereichsfortschritt bis `AC0899`; letzter sichtbarer Eintrag ist ein per-Datei-Warnhinweis `validation_time_budget_exceeded` bei `AC0811_L`.

## Kurzfazit

Run AW liefert erneut einen Prozess-Exit `0`, enthält aber keinen Nachweis eines vollständigen Durchlaufs bis `AC0899`. Damit bleiben N1/N2 weiterhin offen; der Run ist als Zwischenstand mit begrenztem sichtbarem Fortschritt dokumentiert.
