# AC0800..AC0899 Vollbereichslauf – 2026-04-23 (Run V)

- **Datum (UTC):** 2026-04-23
- **Anlass:** N1 erneut ausführen und den Vollbereich `AC0800..AC0899` weiter Richtung Exit-`0`-Nachweis verifizieren.
- **Befehl:**

```bash
python -u -m src.imageCompositeConverter \
  artifacts/images_to_convert \
  --descriptions-path artifacts/images_to_convert/Finale_Wurzelformen_V3.xml \
  --output-dir artifacts/converted_images \
  --start AC0800 \
  --end AC0899 \
  --isolate-svg-render \
  --deterministic-order
```

- **Ausführung:** mit `tee` in `artifacts/converted_images/reports/AC0800_AC0899_batch_2026-04-23_runV.log`
- **Sichtbarer Teilfortschritt im Log:** `AC0800_L`, `AC0800_M`, `AC0800_S`.
- **Crash-Beobachtung bis zum Abbruch:** Kein MuPDF-`stack overflow`/Segfault im sichtbaren Logabschnitt.
- **Laufstatus:** Nach Start von `AC0800_S` blieb der Lauf ohne weiteren sichtbaren Fortschritt; daraufhin manuell per `Ctrl-C` beendet (Shell-Exit-Code `1`, kein finaler Exit-`0`-Nachweis).

## Kurzfazit

Run V liefert weiterhin keinen vollständigen Vollbereichslauf bis `AC0899` mit Exit-Code `0`. N1/N2 bleiben offen; der dokumentierte Fortschritt liegt in diesem Lauf unterhalb des bereits in Run T/U erreichten Bereichs.
