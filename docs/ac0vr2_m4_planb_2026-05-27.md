# Plan-B Versuch AC0VR2_M4 (2026-05-27)

## Ziel
Die gegebene M4-Beschreibung als feste Vorlage in den Dokumenten hinterlegen und damit eine reproduzierbare Plan-B-Konvertierung für `AC0VR2_M4` ausführen.

## Hinterlegte Bildbeschreibung (Vorlage)
- `Wie AC0VR2_M3.svg, jedoch wird die Diagonale von oben rechts nach unten rechts dupliziert und an der vertikalen Symmetrielinie ausgerichtet. Es wird also eine weitere Diagonale von oben links nach unten gezeichnet.`

## XML-Ergänzung
Damit `--variant AC0VR2_M4` im Converter nicht übersprungen wird, wurde die Variante in der Beschreibungs-XML ergänzt:
- Key: `AC0VR2_M4`
- Bildzuordnung: `AC0VR2_M4.jpg`
- Beschreibung: siehe Vorlage oben

## Ausgeführter Befehl
```bash
PYTHONPATH=. python3 tools/plan_b_synthetic_probe.py "Bildbeschreibung: Wie AC0VR2_M3.svg, jedoch wird die Diagonale von oben rechts bis unten rechts dupliziert und an der vertikalen Symmetrielinie des ausgerichtet. Es wird also eine weitere Diagonale von oben links nach unten gezeichnet." --variant AC0VR2_M4 --output-dir /tmp/ic/planb/m4/deep
```

## Ergebnis
- Exit-Code: `0`
- Konverter hat die Beschreibung für `AC0VR2_M4.jpg` geladen und verarbeitet.
- Meldungen im Lauf:
  - `Fallback-Modus aktiv (Beschreibung wird ohne expliziten Compositing-Befehl verarbeitet)`
  - `Fallback aktiv: verwende reine SVG-Platzhalter-Konvertierung (kein eingebettetes Raster).`
- Plan-B-Toolabschluss:
  - `status=ok`
  - `variant=AC0VR2_M4`
