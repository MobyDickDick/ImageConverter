# AC0800..AC0899 Vollbereichslauf – 2026-04-23 (Run U)

- **Datum (UTC):** 2026-04-23
- **Anlass:** N1 weiterführen und erneut versuchen, den geforderten Exit-`0`-Nachweis für B2 ohne `timeout` zu erreichen.
- **Befehl:**

```bash
python -u -m src.imageCompositeConverter \
  artifacts/images_to_convert \
  --descriptions-path artifacts/images_to_convert/Finale_Wurzelformen_V3.xml \
  --output-dir artifacts/converted_images \
  --start AC0800 \
  --end AC0899 \
  --isolate-svg-render \
  --deterministic-order
```

- **Ausführung:** mit `tee` in `artifacts/converted_images/reports/AC0800_AC0899_batch_2026-04-23_runU.log`
- **Sichtbarer Teilfortschritt im Log:** `AC0800_L`, `AC0800_M`, `AC0800_S`, `AC0811_L`.
- **Crash-Beobachtung bis zum Abbruch:** Kein MuPDF-`stack overflow`/Segfault im sichtbaren Logabschnitt.
- **Laufstatus:** Lauf blieb nach dem Start von `AC0811_L` ohne weiteren sichtbaren Fortschritt und wurde daraufhin manuell per `pkill` beendet (kein finaler Exit-`0`-Nachweis).

## Kurzfazit

Run U erweitert den Stabilitätsnachweis nicht über den bereits dokumentierten Bereich aus Run T hinaus und liefert weiterhin keinen vollständigen Vollbereichslauf bis `AC0899` mit Exit-Code `0`. N1/N2 bleiben offen.
