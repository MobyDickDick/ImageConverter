# AC0800..AC0899 Vollbereichslauf – 2026-04-16 (Run G)

- **Datum (UTC):** 2026-04-16
- **Ziel:** B2-Verifikation (Crash-Freiheit im Vollbereich nach B1/B2.1-Guard)
- **Befehl:**

```bash
python -u -m src.imageCompositeConverter \
  artifacts/images_to_convert \
  --descriptions-path artifacts/images_to_convert/Finale_Wurzelformen_V3.xml \
  --output-dir artifacts/converted_images \
  --start AC0800 \
  --end AC0899 \
  --isolate-svg-render \
  --deterministic-order
```

- **Ausführung:** mit `tee` in `artifacts/converted_images/reports/AC0800_AC0899_batch_2026-04-16_runG.log`
- **Ergebnis:** Prozessende mit Exit-Code `0` und ohne nativen MuPDF-Stackoverflow/Segfault.
- **Blocker im Lauf:** Der Batch konnte fachlich nicht evaluieren, da für alle Varianten ein Runtime-Fehler auftrat:
  - `TypeError: prepareRunIterationPipelineLocalsForRunImpl() got an unexpected keyword argument 'prepare_iteration_input_runtime_for_run_fn'`

## Kurzfazit

Der Vollbereichslauf ist in dieser Konfiguration zwar crash-frei auf Prozessebene, aber derzeit durch einen neuen
`TypeError` im Iteration-Pipeline-Wiring blockiert. B2 bleibt damit offen, bis der Lauf ohne diesen Runtime-Fehler
und mit belastbarer Ergebnisauswertung durchläuft.
