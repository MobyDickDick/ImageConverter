# Run CM Summary (2026-05-08)

## Anlass
Erneute Ausführung des dokumentierten N1/N2-Vollbereichslaufs `AC0800..AC0899` in Python `3.10.20`, um den Status reproduzierbar zu aktualisieren und anschließend nur textuelle Doku-Änderungen zu committen.

## Kommando
```bash
set -o pipefail; PYENV_VERSION=3.10.20 timeout 420 python -m src.imageCompositeConverter artifacts/images_to_convert --descriptions-path artifacts/images_to_convert/Finale_Wurzelformen_V3.xml --output-dir artifacts/converted_images --start AC0800 --end AC0899 | tee artifacts/converted_images/reports/AC0800_AC0899_batch_2026-05-08_runCM.log
```

## Ergebnis
- Der äußere `timeout` griff erneut nach 420s.
- Finaler Exit-Code: `124`.

## Kurzfazit
- **N1 bleibt offen**: weiterhin kein Vollbereichsabschluss bis `AC0899` mit Exit `0`.
- **N2 bleibt offen**: stabiler Vollbereichsabschluss weiterhin nicht erreicht.
- Commit enthält ausschließlich Dokumentationsdateien; keine Binärartefakte.
