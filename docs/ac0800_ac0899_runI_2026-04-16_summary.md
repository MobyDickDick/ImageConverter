# AC0800..AC0899 Vollbereichslauf – 2026-04-16 (Run I)

- **Datum (UTC):** 2026-04-16
- **Ziel:** B2-Verifikation nach Korrektur des Run-Preparation-Call-Mappings in `prepareRunIterationPipelineLocalsForRunImpl`
- **Befehl:**

```bash
python -u -m src.imageCompositeConverter \
  artifacts/images_to_convert \
  --descriptions-path artifacts/images_to_convert/Finale_Wurzelformen_V3.xml \
  --output-dir artifacts/converted_images \
  --start AC0800 \
  --end AC0899 \
  --isolate-svg-render \
  --deterministic-order
```

- **Ausführung:** mit `tee` in `artifacts/converted_images/reports/AC0800_AC0899_batch_2026-04-16_runI.log`
- **Ergebnis:** Prozessende mit Exit-Code `0` und ohne nativen MuPDF-Stackoverflow/Segfault.
- **Blocker im Lauf:** Der vorherige `prepare_iteration_input_runtime_for_run_fn`-Fehler tritt nicht mehr auf; stattdessen blockiert jetzt ein nachgelagerter Runtime-Fehler pro Datei:
  - `TypeError: prepareRunIterationPipelineLocalsImpl() got an unexpected keyword argument 'img_path'`

## Kurzfazit

Der erste Run nach dem C1.78-Fix bestätigt die Wirkung auf den ursprünglichen Blocker, zeigt aber einen weiteren
Verdrahtungsfehler eine Ebene tiefer. B2 bleibt offen, bis der Vollbereichslauf ohne diese TypeError-Kette
fachlich durchläuft.
