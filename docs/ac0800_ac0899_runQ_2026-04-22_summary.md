# AC0800..AC0899 Vollbereichslauf – 2026-04-22 (Run Q)

- **Datum (UTC):** 2026-04-22
- **Anlass:** Zusätzlicher B2-Follow-up mit verlängertem Timeout, um den stabilen Startbereich über AC0800 hinaus zu beobachten.
- **Befehl:**

```bash
timeout 240 python -u -m src.imageCompositeConverter \
  artifacts/images_to_convert \
  --descriptions-path artifacts/images_to_convert/Finale_Wurzelformen_V3.xml \
  --output-dir artifacts/converted_images \
  --start AC0800 \
  --end AC0899 \
  --isolate-svg-render \
  --deterministic-order
```

- **Ausführung:** mit `tee` in `artifacts/converted_images/reports/AC0800_AC0899_batch_2026-04-22_runQ.log`
- **Sichtbarer Teilfortschritt im Log:** `AC0800_L`, `AC0800_M`, `AC0800_S`, anschließend Start von `AC0811_L`.
- **Crash-Beobachtung bis zum Timeout:** Kein MuPDF-`stack overflow`/Segfault im sichtbaren Abschnitt.
- **Laufstatus:** Durch `timeout 240` mit Exit-Code `124` beendet; damit weiterhin kein vollständiger Batch-Abschluss und kein finaler Exit-`0`-Nachweis für B2.

## Kurzfazit

Run Q bestätigt erneut eine crash-freie Frühphase mit aktiviertem Isolations-Guard und deterministischer Reihenfolge, jetzt sichtbar bis in die AC0811-Familie. Für B2 bleibt weiterhin ein kompletter Durchlauf bis `AC0899` mit stabilem Exit-Code `0` erforderlich.
