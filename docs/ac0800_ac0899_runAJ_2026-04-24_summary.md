# AC0800..AC0899 Vollbereichslauf – 2026-04-24 (Run AJ)

- **Datum (UTC):** 2026-04-24
- **Anlass:** N1/N2 weiterführen und erneut einen Vollbereichslauf `AC0800..AC0899` ohne `timeout` mit dokumentiertem Prozessstatus erfassen.
- **Befehl:**

```bash
python -u -m src.imageCompositeConverter \
  artifacts/images_to_convert \
  --descriptions-path artifacts/images_to_convert/Finale_Wurzelformen_V3.xml \
  --output-dir artifacts/converted_images \
  --start AC0800 \
  --end AC0899 \
  --isolate-svg-render \
  --deterministic-order
```

- **Ausführung:** mit `tee` in `artifacts/converted_images/reports/AC0800_AC0899_batch_2026-04-24_runAJ.log`
- **Sichtbarer Teilfortschritt im Log:** `AC0800_L`, `AC0800_M`, `AC0800_S`, `AC0811_L`.
- **Crash-Beobachtung bis zum Abbruch:** Kein MuPDF-`stack overflow`/Segfault im sichtbaren Logabschnitt.
- **Laufstatus:** Nach längerem Stillstand ohne weiteren sichtbaren Konsolenfortschritt manuell per `Ctrl-C` beendet (Shell-Exit `1`); kein finaler Exit-`0`-Nachweis dokumentiert.

## Kurzfazit

Run AJ bestätigt erneut einen stabilen Start ohne sichtbaren MuPDF-Crash, erreicht aber weiterhin keinen vollständigen Lauf bis `AC0899` mit Exit-Code `0`. N1/N2 bleiben offen.
