# Run CJ Summary (2026-05-07)

## Anlass
Priorisierte Bearbeitung von **N1** aus `docs/open_tasks.md`: Nach den erfolgreichen T5-Kurzläufen (`Run CH`/`Run CI`) den nächsten Vollbereichslauf `AC0800..AC0899` in der bestätigten Python-`3.10.20`-Umgebung ausführen und Exit/Blockerstatus dokumentieren.

## Kommando
```bash
set -o pipefail; PYENV_VERSION=3.10.20 timeout 420 python -m src.imageCompositeConverter artifacts/images_to_convert --descriptions-path artifacts/images_to_convert/Finale_Wurzelformen_V3.xml --output-dir artifacts/converted_images --start AC0800 --end AC0899 | tee artifacts/converted_images/reports/AC0800_AC0899_batch_2026-05-07_runCJ.log
```

## Log-Pfad
- `artifacts/converted_images/reports/AC0800_AC0899_batch_2026-05-07_runCJ.log`

## Ergebnis
- Laufende Ausgabe blieb während der Beobachtung ohne neue Zeilen sichtbar (typisch bei gepuffertem Output im Langlauf).
- Der äußere `timeout` griff nach 420s; finaler Exit-Code: `124`.

## Kurzfazit
- **N1 bleibt offen**: kein Vollbereichsnachweis bis `AC0899` mit finalem Exit `0`.
- **N2 bleibt offen**: weiterhin kein stabiler Langlaufabschluss über den gesamten Bereich.
- Der Lauf liefert dennoch ein aktuelles N1-Artefakt auf derselben Python-`3.10.20`-Toolchain wie die erfolgreichen T5-Repros.
