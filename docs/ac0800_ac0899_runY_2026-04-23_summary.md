# AC0800..AC0899 Vollbereichslauf – 2026-04-23 (Run Y)

- **Datum (UTC):** 2026-04-23
- **Anlass:** N1 erneut ausführen und den Vollbereich `AC0800..AC0899` weiter Richtung Exit-`0`-Nachweis verifizieren.
- **Befehl:**

```bash
timeout 420 python -u -m src.imageCompositeConverter \
  artifacts/images_to_convert \
  --descriptions-path artifacts/images_to_convert/Finale_Wurzelformen_V3.xml \
  --output-dir artifacts/converted_images \
  --start AC0800 \
  --end AC0899 \
  --isolate-svg-render \
  --deterministic-order
```

- **Ausführung:** mit `tee` in `artifacts/converted_images/reports/AC0800_AC0899_batch_2026-04-23_runY.log`
- **Sichtbarer Teilfortschritt im Log:** `AC0800_L`, `AC0800_M`, `AC0800_S`, `AC0811_L`.
- **Crash-Beobachtung bis zum Abbruch:** Kein MuPDF-`stack overflow`/Segfault im sichtbaren Logabschnitt.
- **Laufstatus:** Lauf durch `timeout 420` beendet; Exit-Code `124` (kein finaler Exit-`0`-Nachweis dokumentiert).

## Kurzfazit

Run Y bestätigt erneut den stabilen Startpfad ohne nativen MuPDF-Crash im frühen AC08-Bereich, liefert aber weiterhin keinen vollständigen Vollbereichslauf bis `AC0899` mit Exit-Code `0`. N1/N2 bleiben offen.
