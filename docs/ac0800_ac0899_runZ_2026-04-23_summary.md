# AC0800..AC0899 Vollbereichslauf – 2026-04-23 (Run Z)

- **Datum (UTC):** 2026-04-23
- **Anlass:** N1-Fortsetzung ohne `timeout`, um den geforderten Exit-`0`-Nachweis für B2 erneut direkt zu erreichen.
- **Befehl:**

```bash
python -u -m src.imageCompositeConverter \
  artifacts/images_to_convert \
  --descriptions-path artifacts/images_to_convert/Finale_Wurzelformen_V3.xml \
  --output-dir artifacts/converted_images \
  --start AC0800 \
  --end AC0899 \
  --isolate-svg-render \
  --deterministic-order
```

- **Ausführung:** mit `tee` in `artifacts/converted_images/reports/AC0800_AC0899_batch_2026-04-23_runZ.log`
- **Sichtbarer Teilfortschritt im Log:** `AC0800_L`, `AC0800_M`, `AC0800_S`, `AC0811_L`.
- **Crash-Beobachtung bis zum Abbruch:** Kein MuPDF-`stack overflow`/Segfault im sichtbaren Logabschnitt.
- **Laufstatus:** Nach längerem Stillstand ohne weiteren sichtbaren Log-Fortschritt manuell via `pkill` beendet; dadurch kein finaler Exit-`0`-Nachweis.

## Kurzfazit

Run Z bestätigt erneut den stabilen Startpfad ohne nativen MuPDF-Crash bis `AC0811_L`, liefert aber weiterhin keinen vollständigen Vollbereichslauf bis `AC0899` mit Exit-Code `0`. N1/N2 bleiben offen.
