# Run CL Summary (2026-05-08)

## Anlass
Priorisierte Bearbeitung der als nächster Schritt dokumentierten schweren Pfade **N2/N1** aus `docs/open_tasks.md`: erneuter Vollbereichslauf `AC0800..AC0899` in der bestätigten Python-`3.10.20`-Umgebung mit klarer Timeout-Grenze und unmittelbarer Run-Dokumentation.

## Kommando
```bash
set -o pipefail; PYENV_VERSION=3.10.20 timeout 420 python -m src.imageCompositeConverter artifacts/images_to_convert --descriptions-path artifacts/images_to_convert/Finale_Wurzelformen_V3.xml --output-dir artifacts/converted_images --start AC0800 --end AC0899 | tee artifacts/converted_images/reports/AC0800_AC0899_batch_2026-05-08_runCL.log
```

## Log-Pfad
- `artifacts/converted_images/reports/AC0800_AC0899_batch_2026-05-08_runCL.log`

## Ergebnis
- Während der Beobachtung keine neuen sichtbaren Zeilen (gepufferter Langlauf-Output wie in den vorherigen Vollbereichsruns).
- Der äußere `timeout` griff erneut nach 420s; finaler Exit-Code: `124`.

## Kurzfazit
- **N1 bleibt offen**: weiterhin kein Vollbereichsnachweis bis `AC0899` mit finalem Exit `0`.
- **N2 bleibt offen**: kein stabiler Vollbereichsabschluss, aber auch kein neuer Crash-/Segfault-Hinweis in diesem Lauf.
- Der Lauf liefert ein aktuelles Vergleichsartefakt für die 3.10-Toolchain und bestätigt den fortbestehenden Laufzeitblocker.
