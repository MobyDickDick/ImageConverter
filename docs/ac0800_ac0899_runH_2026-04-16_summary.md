# AC0800..AC0899 Vollbereichslauf – 2026-04-16 (Run H)

- **Datum (UTC):** 2026-04-16
- **Ziel:** B2-Verifikation nach C1.78-Fix des Run-Preparation-Aufrufmappings
- **Befehl:**

```bash
python -u -m src.imageCompositeConverter \
  artifacts/images_to_convert \
  --descriptions-path artifacts/images_to_convert/Finale_Wurzelformen_V3.xml \
  --output-dir artifacts/converted_images \
  --start AC0800 \
  --end AC0899 \
  --isolate-svg-render \
  --deterministic-order
```

- **Ausführung:** mit `tee` in `artifacts/converted_images/reports/AC0800_AC0899_batch_2026-04-16_runH.log`
- **Ergebnis:** Prozessende mit Exit-Code `0` und ohne nativen MuPDF-Stackoverflow/Segfault.
- **Blocker im Lauf:** Der Batch bleibt fachlich blockiert durch einen Runtime-Fehler pro Datei:
  - `TypeError: prepareRunIterationPipelineLocalsForRunImpl() got an unexpected keyword argument 'prepare_iteration_input_runtime_for_run_fn'`

## Kurzfazit

Der Vollbereichslauf bleibt auf Prozessebene crash-frei, kann aber weiterhin nicht regulär konvertieren, weil das
Run-Preparation-Aufrufmapping zur Laufzeit noch inkonsistent ist. B2 bleibt offen bis ein Vollbereichslauf ohne
TypeError und mit belastbarer Ergebnisauswertung durchläuft.
